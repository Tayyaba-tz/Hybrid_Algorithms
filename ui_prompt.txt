System Architecture Mandate: Hybrid Sorting GUI & BenchmarkingYou are a Staff-Level Python Desktop Application Architect. Your task is to build the presentation and control layer for the Sorting Visualizer using customtkinter and tkinter.Canvas. Wire it directly to the existing engines/ and utils/generator.py files.1. The Renderer (visualizer/renderer.py):Create a CanvasRenderer class that takes a tkinter.Canvas as input.Performance Constraint: Do NOT canvas.delete("all") on every frame. Implement a setup(array) method that creates the rectangle objects once and stores their IDs. Implement an update(state_dict) method that iterates through the array and uses canvas.coords() to adjust heights and canvas.itemconfig() to change colors.Colors: Default bars are Light Blue. Indices in state_dict["comparing"] turn Yellow. Indices in state_dict["swapping"] turn Red. Sorted/finished bars turn Green.2. The Controller (visualizer/controller.py):Create a SortingController class to bridge the GUI and the Engine.It must manage the Python generator returned by the sorting algorithms.Animation Loop: Use Tkinter's canvas.after(delay, self._tick) to pull the next() state from the generator. Do NOT use time.sleep().Implement methods for play(), pause(), reset(array), and set_speed(ms).3. The Main App (main.py):Build a modern Dark Mode customtkinter interface.Left Sidebar:Dropdown: Algorithm (Merge, Quick, Heap, Insertion, Hybrid).Dropdown: Dataset (Random, Sorted, Reversed).Slider: Array Size (10 to 200).Slider: Animation Speed.Buttons: "Generate Data", "Start/Pause Animation".Main Area: A large tkinter.Canvas for the CanvasRenderer.Benchmarking Modal/Frame (Rubric Requirement): Add a "Run Complexity Analysis" button. When clicked, it should run the selected algorithm headless (consuming the generator as fast as possible without updating the Canvas) on a large dataset using time.perf_counter(), and display the execution time and theoretical complexity (e.g., $O(N \log N)$) in a popup or dedicated text box.Ensure all files are cleanly typed, well-commented, and robust against user errors (e.g., clicking "Start" before generating data).